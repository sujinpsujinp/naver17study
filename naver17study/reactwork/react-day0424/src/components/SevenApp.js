import { Alert } from '@mui/material';
import React from 'react';

const SevenApp = () => {
    return (
        <div>
            <Alert severity='success' style={{margin:'15px',fontSize:'20px'}}>SevenAppApp</Alert>
        </div>
    );
};

export default SevenApp;