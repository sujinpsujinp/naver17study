import { Alert } from '@mui/material';
import React from 'react';

const EightApp = () => {
    return (
        <div>
            <Alert severity='success' style={{margin:'15px',fontSize:'20px'}}>EightApp</Alert>
        </div>
    );
};

export default EightApp;