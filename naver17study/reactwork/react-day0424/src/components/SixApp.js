import { Alert } from '@mui/material';
import React from 'react';

const SixApp = () => {
    return (
        <div>
            <Alert severity='success' style={{margin:'15px',fontSize:'20px'}}>SixApp</Alert>
        </div>
    );
};

export default SixApp;