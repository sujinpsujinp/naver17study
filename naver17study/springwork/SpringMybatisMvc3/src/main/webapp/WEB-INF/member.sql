create table member (
    num smallint auto_increment primary key,
    mname varchar(20),
    mpass varchar(20),
    myid varchar(20),
    mhp varchar(20),
    maddr varchar(300),
    mphoto varchar(100),
    gaipday datetime);